spring:
  ai:
    vertex:
      ai:
        gemini:
          project-id: ${GEMINI_PROJECT_ID}
          location: ${GEMINI_LOCATION}
          # 若使用服务账号文件
          # credentials-uri: file:/Users/you/keys/my-service-account.json
          chat:
            options:
              model: gemini-2.0-flash
              temperature: 0.7
              top-k: 40
              top-p: 0.9
